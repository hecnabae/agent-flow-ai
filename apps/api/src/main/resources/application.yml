spring:
  application:
    name: agent-flow-api
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/agent_flow}
      database: agent_flow
  jackson:
    serialization:
      write-dates-as-timestamps: false
server:
  port: 8080
logging:
  level:
    root: INFO
